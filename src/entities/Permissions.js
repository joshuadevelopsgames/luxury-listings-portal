/**
 * @deprecated — Import from '../entities/Capabilities' instead.
 * This file re-exports everything for backwards compatibility.
 */
export {
  CAPABILITIES as PERMISSIONS,
  CAPABILITY_CATEGORIES as PERMISSION_CATEGORIES,
  CAPABILITY_LABELS as PERMISSION_LABELS,
  getPermissionDescription,
} from './Capabilities';
